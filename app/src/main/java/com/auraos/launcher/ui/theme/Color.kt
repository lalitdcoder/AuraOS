package com.auraos.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Aura Core Palette ────────────────────────────────────────────────────────
val AuraPurpleDeep     = Color(0xFF0D0014)  // Background base
val AuraPurpleDark     = Color(0xFF1A0033)  // Surface dark
val AuraPurple         = Color(0xFF6B21A8)  // Primary
val AuraPurpleLight    = Color(0xFFA855F7)  // Primary light / accent
val AuraPurpleMid      = Color(0xFF3B0764)  // Container

// ─── Aura Cyan Accents ────────────────────────────────────────────────────────
val AuraCyan           = Color(0xFF06B6D4)  // Secondary
val AuraCyanLight      = Color(0xFF67E8F9)  // Secondary light
val AuraCyanDark       = Color(0xFF0891B2)  // Secondary dark
val AuraTeal           = Color(0xFF14B8A6)  // Tertiary

// ─── Input / Card Surfaces ────────────────────────────────────────────────────
val AuraSurface        = Color(0xFF1C0A2E)  // Card / input surface
val AuraSurfaceVariant = Color(0xFF27103D)  // Slightly lighter surface
val AuraOutline        = Color(0xFF6B21A8)  // Border color (purple tint)

// ─── Status ───────────────────────────────────────────────────────────────────
val AuraError          = Color(0xFFFF5370)
val AuraSuccess        = Color(0xFF4ADE80)
val AuraWarning        = Color(0xFFFFB74D)

// ─── Text ─────────────────────────────────────────────────────────────────────
val AuraOnPrimary      = Color(0xFFFFFFFF)
val AuraOnSurface      = Color(0xFFEDE9FE)  // Lavender-tinted white
val AuraOnSurfaceDim   = Color(0xFF9CA3AF)  // Dimmed text
val AuraTextPrimary    = Color(0xFFF8F0FF)
val AuraTextSecondary  = Color(0xFFB9A9D1)

// ─── Gradient stops ───────────────────────────────────────────────────────────
val GradientTop        = Color(0xFF0D0014)
val GradientMid        = Color(0xFF1A0033)
val GradientBottom     = Color(0xFF000814)
